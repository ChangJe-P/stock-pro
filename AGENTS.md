# 주몽 프로젝트 작업 규칙

## 역할

- Codex는 기획, 작업 명세, 프로젝트 Skill, 브랜치 준비, 구현 결과 검증을 담당한다.
- Claude Code는 Codex가 준비한 작업 명세를 읽고, 지정된 기능 브랜치에서만 구현과 테스트를 담당한다.
- 한 작업에서 Codex와 Claude Code가 같은 서비스 코드를 동시에 수정하지 않는다.

## 구현 전 필수 확인

Claude Code는 구현을 시작하기 전에 아래 파일을 읽는다.

1. AGENTS.md
2. docs/PROJECT_SPEC.md
3. 현재 작업의 docs/tasks 문서
4. 작업 문서에서 참조한 skills 폴더의 SKILL.md

작업 문서와 일반 지침이 충돌하면 작업 문서의 범위와 완료 기준을 따른다. 범위가 불명확하면 코드를 추측해 확장하지 않고 Codex에 질문한다.

## 제품 경계

- 주몽은 주식 학습용 가상투자 서비스다.
- 가상 현금만 사용한다. 실제 계좌, 실제 주문, 실제 자동매매를 구현하지 않는다.
- MVP는 실제 시장의 일봉 데이터를 사용한다.
- 장 마감 후 결정한 가상 주문은 다음 거래일 시가를 기준으로 체결한다. 같은 날 종가를 보고 같은 종가에 체결하지 않는다.
- 데이터 출처, 기준 시각, 수수료·세금·슬리피지 가정은 기록한다.
- API 키, 계좌 정보, Notion 토큰을 코드·커밋·문서에 넣지 않는다.

## Git 규칙

- main과 dev는 직접 수정하거나 병합하지 않는다.
- Codex가 작업별 브랜치를 dev에서 만든다. Claude Code는 그 브랜치에서만 작업한다.
- Claude Code는 작업 완료 후 논리적인 커밋을 만들 수 있지만 push와 병합은 하지 않는다.
- 기능 범위를 넘는 리팩터링, 의존성 교체, 데이터 소스 추가는 작업 문서에 없으면 하지 않는다.

## 인수인계와 검증

Claude Code는 완료 주장만 하지 않는다. docs/handoffs/T-XXX-claude-handoff.md에 아래를 남긴다.

- 변경·생성 파일 전체 목록
- 요구사항별 구현 위치
- 실행한 명령과 결과
- 테스트·린트 결과
- 가정, 알려진 제한, 미해결 항목

Codex는 인수인계, Git diff, 테스트 결과를 대조해 P0부터 P2까지의 리뷰 결과를 작성한다. 테스트를 실행하지 못했으면 통과로 표현하지 않는다.
